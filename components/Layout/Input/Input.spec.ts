import {describe, expect, test} from 'vitest';
import {render, screen} from '@testing-library/react';
import Input from './Input';


describe('Input component testing', () => {
    test('should receive red border when in state', () => {
        render( )
    })
})